// .vscode/tasks.json

{
    "version" : "2.0.0",
    "tasks": [
        {
            "label": "Swift Build",
            "type": "shell",
            "command": "swift build -Xswiftc -suppress-warnings",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}